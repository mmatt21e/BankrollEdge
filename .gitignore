*.iml
.gradle
local.properties
/.idea
.DS_Store
build/
/captures
.externalNativeBuild
.cxx
*.apk
*.aab
/app/release

# AI assistant local files
.claude/
.codex/
CLAUDE.md
CODEX.md
AGENTS.md
*.claude
*.codex

# Signing — never commit the keystore or its passwords
*.jks
*.keystore
keystore.properties

# Web (PWA)
web/node_modules/
web/dist/
web/dev-dist/

# Prebuilt binaries stay out of git — attach APKs to GitHub Releases instead.
dist/

# Root-level node_modules (tooling accidents) — the web app uses web/node_modules
node_modules/
